# check-determinism allowlist — one justified line per verified-safe site.
# Format: <file>:<line>|<source line>|<justification>
#   <file>:<line>   the emitted violation's location (its first two fields).
#   <source line>   the emitted violation's source text after the second
#                   colon, trimmed of leading/trailing spaces and tabs.
#                   Colons, braces, and trailing comments are preserved.
#   <justification> free-text reason the site is order-independent — never
#                   compared, but required.
# `|` is never escaped: any field (including a candidate source) containing
# an unescaped `|` is malformed, and every data line must have exactly three
# non-empty fields. Validation runs in BOTH directions: an unallowlisted
# candidate fails, and so does a dead (matching no candidate), duplicate, or
# malformed entry. Re-run after editing nearby code: line numbers rot; a
# stale entry here is itself a finding (the lint fails on it when the site
# moves).

internal/compiler/communities.go:113|for li := range byLevel {|populates keyed lookup maps only (commOfEntity/counts); output rows come from an ordered SELECT
internal/compiler/worker_cycle.go:175|for p := range paths {|paths set → allPaths slice for an IN-clause UPDATE (set semantics)
internal/compiler/worker_cycle.go:184|for tier, items := range claimed {|max-reduction to compute maxTier only (order-independent)
internal/compiler/worker_cycle.go:250|for _, it := range claimed[0] {|claimed[0] fixed-key slice; doc-finished events in stable slice order
internal/compiler/worker_cycle.go:262|for _, it := range claimed[1] {|claimed[1] fixed-key slice; doc-finished events in stable slice order
internal/compiler/worker_cycle.go:274|for _, it := range claimed[3] {|claimed[3] fixed-key slice; builds toProcess in stable slice order
internal/compiler/worker_cycle.go:318|for _, it := range claimed[3] {|claimed[3] fixed-key slice; MarkPass per item (idempotent)
internal/compiler/worker_cycle.go:329|for _, it := range claimed[3] {|claimed[3] fixed-key slice; errored classification per item (order-independent)
internal/compiler/worker_cycle.go:358|for p := range paths {|per-path release loop — each path released independently, order-independent
internal/compiler/worker_cycle.go:404|for p := range readFailed {|counting loop (unaccounted++ only), order-independent
internal/compiler/worker_cycle.go:513|for _, it := range claimed {|claimed is a []CompileItem slice parameter here (shadowed), not the worker map
internal/compiler/worker_cycle.go:527|for _, items := range claimed {|claimedItemFor membership scan — first match by SourcePath wins, order-independent
internal/compiler/worker_cycle.go:465|for _, it := range claimed {|claimed is a []CompileItem slice param; builds paths for one IN-clause Heartbeat (set semantics)
internal/compiler/worker_cycle.go:490|for _, it := range claimed {|claimed is a []CompileItem slice param; per-item release, order-independent
internal/compiler/compilekey.go:214|for name := range models {|sort.Strings(names) immediately after the loop
internal/compiler/write.go:524|for name := range want {|prefix-match membership loop, produces no ordered output
internal/compiler/write.go:827|for other := range bySource[s] {|sort.Slice(related) immediately after the shared-count loop
internal/compiler/write.go:838|for name := range shared {|same: the related slice is sorted by count desc, name asc right below
internal/compiler/concepts.go:407|for k, v := range seen {|purge-and-re-register over map keys — set semantics, all keys handled regardless of order
internal/compiler/resolve_entities.go:208|for typ, entities := range byType {|populates keyed df-count maps only (lookups by key)
internal/compiler/resolve_entities.go:230|for tok, n := range df {|membership map (ok[tok]) — read back by key only
internal/compiler/resolve_entities.go:401|for label := range seen {|claimed-set membership writes
internal/compiler/resolve_entities.go:430|for _, c := range out {|range over SLICE (shadowed `out`), not the map of the same name
internal/compiler/resolve_entities.go:435|for _, e := range labels {|range over SLICE `labels` (shadow), not a map
internal/compiler/resolve_entities.go:545|for _, t := range norm[seed.ID] {|norm[id] slice membership compare
internal/compiler/resolve_entities.go:549|for _, u := range norm[c.ID] {|norm[id] slice membership compare
internal/compiler/resolve_entities.go:580|for _, l := range labels {|labels slice already sorted at :577 immediately before
internal/compiler/resolve_entities.go:1272|for _, n := range adj[cur] {|adj[cur] is a slice lookup; BFS result sorted at :1281
internal/compiler/watch.go:206|for path := range snapshot {|sort.Strings(changed) after both snapshot loops (SPEC-04 Task 3)
internal/compiler/resolve_entities.go:493|for _, c := range byType[seed.Type] {|byType[seed.Type] is a slice lookup, not a map-order iteration
internal/compiler/pipeline.go:1391|for _, path := range pathByID {|pathByID values → pending slice, sorted immediately at :1394
internal/compiler/pipeline.go:1511|for id, path := range expected {|expected map → missing list for an error report only (not an artifact)
internal/compiler/diff.go:128|for path, info := range current {|Added/Modified sorted at :140-141, Removed at :149 (pre-existing AGENTS.md rule 6)
internal/compiler/diff.go:144|for path := range mf.Sources {|Removed sorted at :149 (pre-existing AGENTS.md rule 6)
internal/compiler/migrate_checkpoint.go:173|for path := range mf.Sources {|collects mf.Sources keys into a slice that is sort.Strings'd immediately after (PopulateFromManifest) — order-independent collection
internal/compiler/fullpipeline.go:524|for _, srcPath := range mf.Concepts[ar.ConceptName].Sources {|mf.Concepts[name].Sources is a slice lookup, not a map-order iteration
internal/compiler/dedup.go:142|for existing, existingVec := range dc.cache {|canonical tie-break (Task 3) makes the result order-independent
internal/manifest/merge.go:24|for k, s := range m.Sources {|copy loop into a keyed map (json-sorted on save)
internal/manifest/merge.go:32|for k, con := range m.Concepts {|copy loop into a keyed map (json-sorted on save)
internal/manifest/manifest.go:273|for path, s := range m.Sources {|returns a map (PendingSources) — no slice order to leak
internal/manifest/manifest.go:284|for name, c := range m.Concepts {|ArticlesFromSource sorted before return (SPEC-04 Task 3)
internal/ontology/community.go:101|for id := range existing {|delete-by-key set semantics inside one tx
internal/ontology/temporal.go:62|for a := range edges {|edges map read; forms sorted at return (SPEC-04 D1 fix)
internal/ontology/temporal.go:68|for f := range forms {|forms slice sorted at return (SPEC-04 D1 fix)
internal/mirror/changes.go:242|for rel, sha := range present {|changes slice sorted by Path at :271
internal/mirror/changes.go:256|for name, sha := range vectorsPresent {|changes slice sorted by Path at :271
internal/mirror/changes.go:273|for rel := range d.cache {|cache prune by key delete (set semantics)
internal/mirror/verify.go:153|for k := range referenced {|keys sorted immediately after (sort.Strings)
internal/mirror/verify.go:111|for gen := range rotated {|gens sorted immediately after this loop (SPEC-04 D1 fix)
internal/pack/merge.go:518|for _, f := range files {|files is a []string parameter (slice), not a map
internal/wiki/reconcile.go:180|for name, c := range mf.Concepts {|expectedOutputs sorted by path before return (SPEC-04 D1 fix)
internal/wiki/reconcile.go:186|for src, s := range mf.Sources {|expectedOutputs sorted by path before return (SPEC-04 D1 fix)
internal/sourcedate/backfill.go:175|for p := range m.Sources {|this IS the sort helper (sortedSourcePaths)
internal/sourcedate/backfill.go:184|for n := range m.Concepts {|this IS the sort helper (sortedConceptNames)
internal/parity/normalize.go:79|for p, src := range mf.Sources {|zeroing timestamps in a keyed map (normalized output is json-sorted)
internal/parity/normalize.go:84|for name, c := range mf.Concepts {|zeroing timestamps in a keyed map (normalized output is json-sorted)
internal/compiler/migrate_checkpoint.go:61|for path := range mf.Sources {|paths slice sorted at :64 before upserts (SPEC-04 D1 fix)
